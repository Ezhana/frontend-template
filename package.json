{
  "name": "frontend-template",
  "private": true,
  "description": "Frontend project templates.",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "build:templates": "pnpm --dir apps/vue-vite build && pnpm --dir apps/react-vite build",
    "prepare": "simple-git-hooks"
  },
  "author": "Ezhana",
  "license": "MIT",
  "packageManager": "pnpm@12.4.2",
  "engines": {
    "node": ">=24 <25"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm exec lint-staged"
  },
  "devDependencies": {
    "@ezhana/eslint-config-base": "workspace:*",
    "@ezhana/prettier-config": "workspace:*",
    "eslint": "catalog:",
    "lint-staged": "17.5.1",
    "prettier": "catalog:",
    "simple-git-hooks": "2.14.0",
    "typescript": "catalog:"
  }
}
